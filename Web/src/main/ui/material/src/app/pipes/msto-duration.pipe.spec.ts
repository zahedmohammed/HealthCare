import { MstoDurationPipe } from './msto-duration.pipe';

describe('MstoDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new MstoDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
